# Jetson + Livox Mid-360 建圖系統 — 問題排除紀錄

2026-08-04 ~ 08-05 實測。平台:Jetson Orin Nano Super 8GB / Ubuntu 22.04 / ROS 2 Humble /
Livox Mid-360 / FAST-LIO / slam_toolbox,顯示端為 Windows 筆電(WSL2 + RViz2、瀏覽器)。

---

## 一、症狀 → 根因對照

同一個根因常常表現成好幾種完全不同的症狀,這張表是照「你會先看到什麼」來排的。

| # | 你看到的症狀 | 真正的根因 | 診斷關鍵 |
|---|---|---|---|
| 1 | RViz2 一片全黑 | FAST-LIO 位姿發散,點雲被畫在 318 km 外 | `tf2_echo camera_init body` 回傳 `[38138, 22659, -318147]` |
| 2 | FAST-LIO 突然消失 | 被 OOM killer 殺掉 | `exit code -9`;`dmesg` 有 `Out of memory: Killed process (fastlio_mapping)` |
| 3 | 記憶體 50 MB/分 無限成長 | `dense_publish_en` 造成 **DDS 背壓**,不是地圖問題 | 四個地圖參數全調過都無效;關掉後 RSS 立刻持平 |
| 4 | 靜止時位置飄到 2.8 m | `cube_side_length` / `det_range` 被改成壞組合 | 還原後靜止位置 0.015 m |
| 5 | `IMU and LiDAR not Synced` | **記憶體壓力的症狀**,不是時鐘問題 | IMU 時間戳凍結而 lidar 持續前進 = callback 被餓死 |
| 6 | 跨機 DDS「先通後斷」 | Fast DDS 不交付,但封包**有**到 | tcpdump 15 秒收到 6648 個封包,`ros2 topic hz` 卻是 0 |
| 7 | 只有 `/cloud_registered` 收不到 | Jetson `rmem_max` 只有 208 KB,裝不下 0.42 MB 訊息 | `/Odometry`、`/tf`、`/cloud_registered_body` 全都正常 |
| 8 | slam_toolbox 整個消失 | 位姿圖爆炸到 2.67 GB,觸發全域 OOM | 我把 `minimum_travel_distance` 從 0.2 調到 0.05 |
| 9 | 2D 地圖不更新 | **正確行為**,光達沒動就不收新掃描 | 這不是故障,不要去調門檻 |
| 10 | 3D 畫面每 5 秒整片閃 | 衰減重送先送 `cleared`,清空到填滿之間有空窗 | 15 萬點要好幾個 frame 才送完 |
| 11 | 點很大顆看不出物體 | `gl_PointSize` 在 60 m 內全部撞到 14 px 上限 | `uSize 3.0 × 280 / 距離`,分母要 60 才降到上限以下 |
| 12 | 走過的人留下永久殘影 | 檢視器只累積不移除 | 設計如此,需要加確認門檻與時間衰減 |
| 13 | 2D 地圖按鈕按不動 | `#wrap` 滿版透明容器蓋在面板上攔截點擊 | 兩者都是 `position:fixed` 且無 `z-index`,DOM 後者在上 |

---

## 二、FAST-LIO 設定:最終值與理由

`~/ws_livox/install/fast_lio/share/fast_lio/config/mid360.yaml`
**src 和 install 是兩份獨立檔案(不是 symlink),兩邊都要改。**

| 參數 | 原廠 | 最終 | 為什麼 |
|---|---|---|---|
| `pcd_save_en` | true | **false** | `interval: -1` 會把每一幀永久累積在 RAM,config 註解自己就寫 "may lead to memory crash" |
| `dense_publish_en` | true | **false** | 記憶體洩漏真凶,見下節 |
| `filter_size_surf` | 0.2 | **0.1** | 補回關掉 dense 損失的細節 |
| `path_en` / `map_en` | true | false | 純減負載,網頁端用不到 |
| `time_sync_en` | false | **維持 false** | 改成 true 會觸發 `lidar loop back`,行程活著卻完全不輸出 |
| `det_range` | 100.0 | **維持 100.0** | 動它會造成靜止漂移 |
| `cube_side_length` | 1000.0 | **維持 1000.0** | 同上,且必須跟 `det_range` 成對看 |
| `filter_size_map` | 0.2 | **維持 0.2** | 追記憶體時不該動這個 |

### 記憶體洩漏的真正機制

不是 SLAM 地圖成長,是 **DDS 背壓**:

```
/cloud_registered  0.42 MB/則 × 10 Hz = 4.2 MB/s,RELIABLE QoS
        ↓
網頁檢視器(Python,39% CPU)+ zenoh bridge 都排不掉
        ↓
reliable writer 必須保留未被確認的樣本
        ↓
Fast DDS writer history 在 FAST-LIO 行程內累積,實測漏 833 KB/s
        ↓
45 分鐘頂到 3 GB
```

**頂到上限後行程不會死,而是卡在天花板 thrashing**,200 Hz 的 IMU callback 被餓死 →
`not Synced` → 配準失敗 → 位姿飄到 33 m 外。看起來像 SLAM 發散,其實是記憶體問題。

驗證:`dense_publish_en: false` 後 RSS **完全持平在 153 MB**(4 分鐘 0 成長)。

### `det_range` 與 `cube_side_length` 是一組的

FAST-LIO 只把地圖點留在邊長 `cube_side_length` 的立方體內。當機器人靠近邊界到
`1.5 × det_range` 以內,立方體就滑動並刪掉外面的點(`lasermap_fov_segment()`)。

| 組合 | 半邊長 vs 觸發距離 | 結果 |
|---|---|---|
| `1000 / 100`(原廠) | 500 > 150 | 室內永遠不觸發,原廠就是要它幾乎不動 |
| `200 / 30` | 100 > 45 | 也不觸發 |
| **`200 / 100`(壞組合)** | 100 < 150 | **每一幀都在滑動刪點** |

壞組合的後果:初始化那幾秒地圖點一直被刪,收斂不到正確位置 →
啟動後固定偏移 0.9 m,長時間再飄到 2.8 m。還原後靜止位置 **0.015 m**。

---

## 三、系統層設定

| 項目 | 值 | 為什麼 |
|---|---|---|
| `/etc/sysctl.d/60-ros2-lidar.conf` | `rmem_max = 8388608` | 出廠 208 KB 裝不下 0.42 MB 的點雲訊息,分片收不齊就被丟。**改完必須重啟 bridge**,socket buffer 只在啟動時讀一次 |
| FAST-LIO cgroup | `MemoryMax=3G` | 不設的話全域 OOM killer 會掃到不相干行程(實測連 containerd 都被 invoke) |
| slam_toolbox cgroup | `MemoryMax=1500M` | 同上 |

用法:`systemd-run --user --scope -p MemoryMax=3G --unit=<name> --quiet <指令>`

---

## 四、slam_toolbox

| 項目 | 結論 |
|---|---|
| `minimum_travel_distance` / `minimum_travel_heading` | **維持 0.2,不要調低。** 降到 0.05 會讓靜止抖動每分鐘塞 600 個位姿圖節點,15 分鐘漲到 2.67 GB 被全域 OOM 殺掉 |
| 「光達不動時地圖不更新」 | **正確行為,不是故障。** 要看到地圖延伸就得真的移動 |
| `pause_new_measurements` | 名義上是 toggle,**實測只能暫停不能恢復**(連叫三次都回 `status=True`,`ros2 service call` 直接叫也一樣)。介面上不要放這個按鈕 |
| `save_map` | 可用。吃 `std_msgs/String name`,剛重啟還沒建圖時會回 `result=255` |
| 清空地圖 | 沒有 reset 服務,只能重啟節點 |

### TF 接合

FAST-LIO 的 frame 名稱是寫死的 `camera_init → body`,而 Nav2/slam_toolbox 要
`map → odom → base_link`。直接跑會是兩棵不相連的樹,要用兩個 identity static transform 接:

```
map ──(slam_toolbox 發)── odom ──(static)── camera_init
                                                │ (FAST-LIO 發)
                                               body
                                                │ (static)
                                            base_link
```

另外 `pointcloud_to_laserscan` 要吃 **`/cloud_registered_body`**,不能用
`/cloud_registered`(那是世界座標系,需要的是感測器座標系的雲)。

---

## 五、跨機傳輸:從 DDS 換成 zenoh

直連 DDS 會「先通後斷」—— 剛重啟時 10 Hz,幾分鐘後衰減到 0。
tcpdump 兩端對照確認封包**有**進到 WSL(15 秒 6648 個),但 Fast DDS 就是不交付。
已排除防火牆(封包有到)和 `/dev/shm` 耗盡(只用 2%)。

```
Jetson (ROS_DOMAIN_ID=0)                    WSL (ROS_DOMAIN_ID=1)
  livox ─┐                                    ┌─ RViz2
  FAST-LIO ─┼─ zenoh-bridge ══ TCP 7447 ══>  zenoh-bridge ─┘
  web viewer ─┘  -l tcp/0.0.0.0:7447          -e tcp/192.168.40.98:7447
```

兩邊 domain 刻意分開,讓那條會衰減的 UDP 路徑徹底消失。
只有一條 **WSL 主動撥出**的 TCP,不需要任何 Windows 入站防火牆規則。

| 注意事項 | 說明 |
|---|---|
| 版本 | 兩端必須一致(此處 1.9.0) |
| 安裝來源 | Jetson 連不到 `download.eclipse.org`,走 GitHub release 二進位檔,用 pscp 送 aarch64 版 |
| 啟動順序 | Jetson 端 bridge 要排在 **FAST-LIO 之後**,否則探索不到 publisher |
| 路由延遲 | 新訂閱者建立後**前 ~20 秒收不到資料**,這是正常的。測 `ros2 topic hz` 要給 40 秒而不是 10 秒 |
| 短命訂閱者 | `ros2 topic echo --once` 只活 0.7 秒,等不到路由建立,永遠測不到大 topic |

---

## 六、網頁檢視器

### 3D 點雲(`~/lidar_web/`,:8080)

| 問題 | 解法 |
|---|---|
| 點太大顆,看不出物體 | `gl_PointSize` 上限 14 → **6 px**,`uSize` 3.0 → **0.06**(原本 `uSize × 280 / 距離` 在 60 m 內全部飽和,遠近一律最大顆);滑桿範圍同步重新標定 |
| 細節不足 | 伺服器端體素 4 cm → **2 cm** |
| 走動的人留下永久殘影 | **確認門檻**:體素要被看到 3 幀才進圖(人走過停留不到 0.3 秒,進不來)+ **時間衰減**:25 秒沒再被觀測就移除 |
| 衰減重送時整片閃爍 | 不送 `cleared`,改用 `begin_rebuild`/`end_rebuild`:**原地從 0 覆蓋,`drawRange` 維持舊值,收完才一次切換**。新集合是舊集合的子集,所以覆蓋過程視覺無縫 |
| 重送與即時點搶游標 | 重送期間伺服器暫停推即時點 |

### 2D 佔據網格(`~/slam2d/`,:8090)

| 問題 | 解法 |
|---|---|
| 收不到 `/map` | slam_toolbox 用 **TRANSIENT_LOCAL** 發佈,訂閱端 QoS 不相符會一則都收不到 |
| 按鈕按不動 | `#panel` 加 `z-index:10`,`#wrap` 設 `pointer-events:none`、只有 canvas 收事件 |

---

## 七、遠端操作的兩個坑

| 坑 | 症狀 | 解法 |
|---|---|---|
| `echo <base64> \| base64 -d \| bash` 會弄壞**中文註解** | bash 在括號處報 syntax error | 遠端腳本一律純 ASCII,或用 `pscp` 送檔案再執行 |
| `echo pw \| sudo -S tee file <<EOF` | `Sorry, try again` ×3 | heredoc 會蓋掉 sudo 的 stdin。先寫到 `/tmp` 再 `sudo cp` |

---

## 八、驗證方法

**不要只看 3 分鐘。** 壞掉那一輪前 3 分鐘警告數也是 0,第 7 分鐘才開始刷,13 分鐘才 OOM。

FAST-LIO 健康的判準(四個計數器全部為 0):

```bash
grep -c 'not Synced'   /tmp/fastlio.log
grep -c 'loop back'    /tmp/fastlio.log
grep -c 'No Effective' /tmp/fastlio.log
grep -c 'Leaf size'    /tmp/fastlio.log
```

靜止時位置應該在 **公分等級**。跑到公尺就是有問題。

現況(還原全部參數後):RSS 157–158 MB 持平、離原點 **0.015 m**、`/cloud_registered` 10.0 Hz。
