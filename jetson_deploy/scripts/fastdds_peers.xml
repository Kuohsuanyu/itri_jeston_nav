<?xml version="1.0" encoding="UTF-8" ?>
<!--
  Jetson 端的 Fast DDS 設定 —— 讓探索走單播,不要只靠 multicast。

  ── 為什麼需要 ──────────────────────────────────────────────────
  DDS 的參與者探索預設走 multicast(239.255.0.1)。WiFi 的 AP 對 multicast
  幾乎都會降速到最低基本速率、而且不保證送達,結果是「時好時壞」:

    2026-08-11 實測
      Jetson 收得到底盤的資料      /tf 輪子 5 Hz、/joint_states 7.7 Hz  ✓
      ros2 node list 看得到底盤    ✗ 一個都沒有
      zenoh bridge 探索到底盤      ✗ 所以不幫 /joint_states /odom
                                     /battery_state 建路由

  資料在流是因為訂閱者和發布者曾經配對成功(端點探索完成過);
  但**節點層級**的資訊要靠持續的 multicast 公告維持,那一半掉了。
  凡是「按探索到的節點做事」的東西都會出錯 —— zenoh bridge 是,
  ros2 node list 也是。這已經誤導我們好幾次,誤判成「底盤沒上線」。

  ── 做法 ────────────────────────────────────────────────────
  initialPeersList 直接寫死底盤的 IP。Fast DDS 會**額外**用單播對那個位址
  發參與者公告,不必等 multicast。multicast 保持開啟,兩條路並用。

  ★ 底盤換 IP 的話這裡要改。IP 的正本在 robot_env.sh(BASE_IP),
    但 XML 沒辦法讀 shell 變數,所以這裡是**唯一一份需要手動同步的複本**。
    改 robot_env.sh 的時候記得回來改這裡。

  ★ 2026-08-13 起底盤走有線 192.168.0.101。無線 192.168.40.160 也留著:
    initialPeersList 只是「額外用單播發探索公告」,多寫一個位址的成本是
    每 100 ms 幾十個位元組,但少寫一個就可能在切換網路時整個探索不到。
    兩個都列,哪一條通就走哪一條。

  ★ 這裡列出位址**不等於**資料會走那條路。實際走哪條由對端公告的
    locator 決定 —— 樹莓派兩張網卡都開著時,它會同時公告有線和無線,
    對端挑哪個不保證。要確定走有線,樹莓派那邊要:
        sudo ip link set wlan0 down

  ★ 這個檔要靠 FASTRTPS_DEFAULT_PROFILES_FILE 指定,而且是**每個行程各自**
    讀的 —— 只有設了環境變數的那個行程會生效。所以想讓 ros2 node list
    也看得到底盤,那個終端機也要 export。
-->
<dds xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <profiles>

    <!-- ★ 2026-08-14:DDS 只走有線,本機交給共享記憶體。
         ─────────────────────────────────────────────────────────
         為什麼要限制:樹莓派兩張網卡都開著就同時公告兩個 locator,
         對端挑哪一條不保證,實測流量被切成兩半。而兩條差三個數量級:
             有線 192.168.0.101   0% 遺失  平均 0.296 ms  抖動 0.032 ms
             無線 192.168.40.160  0% 遺失  平均 218 ms    抖動 215 ms
         挑到無線就是「有時候延遲」和「旋轉不穩」。

         interfaceWhiteList **只對自訂 transport 生效**,所以一定要
         useBuiltinTransports=false。但關掉內建的同時也關掉了內建的 SHM,
         而 SHM 正是同機不同行程之間在用的 —— 沒補回來就會變成
         「遠端正常、本機反而死掉」。

         ★ 我第一次補的 SHM 描述器只寫了 <type>SHM</type>,沒有任何參數,
           結果 SHM 沒真的生效,本機退回被白名單擋住的 UDP。症狀:
               /tf_static  base_link <- base_footprint(來自底盤,遠端) ✓
               /tf_static  box_link / body / camera_link(本機)        ✗
               /scan 完全沒有(p2l 和 FAST-LIO 都在本機)
           而三個 static publisher 都活著,log 是 "Spinning until stopped"。

         補上 segment_size / port_queue_capacity / healthy_check_timeout_ms
         之後就正常了。驗證方法必須用**兩個獨立行程** —— 同一個行程裡的
         兩個節點走 intra-process,根本不經過 transport,測了會過但沒意義。

         白名單只留 192.168.0.100:本機靠 SHM,不需要 127.0.0.1。
         (加了也能通,實測 270 則;不加比較乾淨。)

         ★ 筆電不受影響 —— zenoh bridge 對筆電走 TCP 7447,不是 DDS。 -->
    <transport_descriptors>
      <transport_descriptor>
        <transport_id>shm_local</transport_id>
        <type>SHM</type>
        <segment_size>4194304</segment_size>
        <port_queue_capacity>1024</port_queue_capacity>
        <healthy_check_timeout_ms>1000</healthy_check_timeout_ms>
      </transport_descriptor>
      <transport_descriptor>
        <transport_id>udp_wired_only</transport_id>
        <type>UDPv4</type>
        <sendBufferSize>8388608</sendBufferSize>
        <receiveBufferSize>8388608</receiveBufferSize>
        <interfaceWhiteList>
          <address>192.168.0.100</address>
        </interfaceWhiteList>
      </transport_descriptor>
    </transport_descriptors>

    <participant profile_name="jetson_participant" is_default_profile="true">
      <rtps>
        <!-- 點雲每則 0.36 MB,遠超過單一 UDP datagram,一定會分片。
             預設緩衝裝不下所有分片,症狀是「小訊息正常、點雲收不到」。 -->
        <sendSocketBufferSize>8388608</sendSocketBufferSize>
        <listenSocketBufferSize>8388608</listenSocketBufferSize>

        <!-- 見檔頭的 transport_descriptors 說明。 -->
        <useBuiltinTransports>false</useBuiltinTransports>
        <userTransports>
          <transport_id>shm_local</transport_id>
          <transport_id>udp_wired_only</transport_id>
        </userTransports>

        <builtin>
          <initialPeersList>
            <!-- 底盤樹莓派 itri-base:有線(主要)-->
            <locator>
              <udpv4>
                <address>192.168.0.101</address>
              </udpv4>
            </locator>
            <!-- ★ 底盤的無線位址已移除:白名單擋掉之後那個位址送不出去,
                 留著只會讓 Fast DDS 對一條死路重試。 -->
            <!-- 自己,確保本機行程之間一定看得到 -->
            <locator>
              <udpv4>
                <address>127.0.0.1</address>
              </udpv4>
            </locator>
          </initialPeersList>
        </builtin>
      </rtps>
    </participant>

  </profiles>
</dds>
