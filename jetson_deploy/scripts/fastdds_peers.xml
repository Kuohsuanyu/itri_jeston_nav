<?xml version="1.0" encoding="UTF-8" ?>
<!--
  Jetson 端的 Fast DDS 設定 —— 讓探索走單播,不要只靠 multicast。

  ── 為什麼需要 ──────────────────────────────────────────────────
  DDS 的參與者探索預設走 multicast(239.255.0.1)。WiFi 的 AP 對 multicast
  幾乎都會降速到最低基本速率、而且不保證送達,結果是「時好時壞」:

    2026-08-11 實測
      Jetson 收得到底盤的資料      /tf 輪子 5 Hz、/joint_states 7.7 Hz  ✓
      ros2 node list 看得到底盤    ✗ 一個都沒有
      zenoh bridge 探索到底盤      ✗ 所以不幫 /joint_states /odom
                                     /battery_state 建路由

  資料在流是因為訂閱者和發布者曾經配對成功(端點探索完成過);
  但**節點層級**的資訊要靠持續的 multicast 公告維持,那一半掉了。
  凡是「按探索到的節點做事」的東西都會出錯 —— zenoh bridge 是,
  ros2 node list 也是。這已經誤導我們好幾次,誤判成「底盤沒上線」。

  ── 做法 ────────────────────────────────────────────────────
  initialPeersList 直接寫死底盤的 IP。Fast DDS 會**額外**用單播對那個位址
  發參與者公告,不必等 multicast。multicast 保持開啟,兩條路並用。

  ★ 底盤換 IP 的話這裡要改。IP 的正本在 robot_env.sh(BASE_IP),
    但 XML 沒辦法讀 shell 變數,所以這裡是**唯一一份需要手動同步的複本**。
    改 robot_env.sh 的時候記得回來改這裡。

  ★ 2026-08-13 起底盤走有線 192.168.0.101。無線 192.168.40.160 也留著:
    initialPeersList 只是「額外用單播發探索公告」,多寫一個位址的成本是
    每 100 ms 幾十個位元組,但少寫一個就可能在切換網路時整個探索不到。
    兩個都列,哪一條通就走哪一條。

  ★ 這裡列出位址**不等於**資料會走那條路。實際走哪條由對端公告的
    locator 決定 —— 樹莓派兩張網卡都開著時,它會同時公告有線和無線,
    對端挑哪個不保證。要確定走有線,樹莓派那邊要:
        sudo ip link set wlan0 down

  ★ 這個檔要靠 FASTRTPS_DEFAULT_PROFILES_FILE 指定,而且是**每個行程各自**
    讀的 —— 只有設了環境變數的那個行程會生效。所以想讓 ros2 node list
    也看得到底盤,那個終端機也要 export。
-->
<dds xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <profiles>

    <participant profile_name="jetson_participant" is_default_profile="true">
      <rtps>
        <!-- 點雲每則 0.36 MB,遠超過單一 UDP datagram,一定會分片。
             預設緩衝裝不下所有分片,症狀是「小訊息正常、點雲收不到」。 -->
        <sendSocketBufferSize>8388608</sendSocketBufferSize>
        <listenSocketBufferSize>8388608</listenSocketBufferSize>

        <!-- ★ 保持 true。2026-08-14 又試了一次「自訂 transport + interfaceWhiteList」
             ——文件上那是限制 DDS 只走特定網卡的標準做法,而且確實有效:
             Jetson 從無線送出的 DDS 封包從 869 個掉到 1 個。
             **但它把跨行程的本機通訊弄壞了。**

             實測(useBuiltinTransports=false,userTransports = SHM + 白名單 UDP):
                 /tf_static  base_link <- base_footprint   來自底盤(遠端)  ✓ 收得到
                 /tf_static  box_link / body / camera_link  本機同機行程      ✗ 收不到
                 三個 static publisher 都活著,log 是 "Spinning until stopped"
                 /scan 完全沒有(p2l 和 FAST-LIO 都是本機)

             遠端正常、本機不通。中間我做過一次「兩個節點互通」的隔離測試會過,
             那是誤導 —— 那兩個節點在**同一個行程**裡,intra-process 根本不經過
             transport。要驗必須用兩個獨立行程。

             所以這條路不通。要把底盤逼到有線,只能在**樹莓派**那端關掉 wlan0。 -->
        <useBuiltinTransports>true</useBuiltinTransports>

        <builtin>
          <initialPeersList>
            <!-- 底盤樹莓派 itri-base:有線(主要)-->
            <locator>
              <udpv4>
                <address>192.168.0.101</address>
              </udpv4>
            </locator>
            <!-- 同一台的無線位址,有線沒接好時的退路 -->
            <locator>
              <udpv4>
                <address>192.168.40.160</address>
              </udpv4>
            </locator>
            <!-- 自己,確保本機行程之間一定看得到 -->
            <locator>
              <udpv4>
                <address>127.0.0.1</address>
              </udpv4>
            </locator>
          </initialPeersList>
        </builtin>
      </rtps>
    </participant>

  </profiles>
</dds>
